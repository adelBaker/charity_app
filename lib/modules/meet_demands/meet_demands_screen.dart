import 'package:flutter/cupertino.dart';

class MeetDemandsScreen extends StatelessWidget {
  const MeetDemandsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("MeetDemandsScreen"));
  }
}
