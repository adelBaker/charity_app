import 'package:flutter/material.dart';

class AdsForNeedScreen extends StatelessWidget {
  const AdsForNeedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ads for need screen"),

    );
  }
}
