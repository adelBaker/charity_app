import 'package:flutter/material.dart';

class AdsForSurplusScreen extends StatelessWidget {
  const AdsForSurplusScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ads for surplus screen"),

    );

  }
}
